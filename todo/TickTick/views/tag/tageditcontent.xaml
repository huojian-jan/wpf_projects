<UserControl x:Class="ticktick_WPF.Views.Tag.TagEditContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ticktick_WPF.Views.Tag"
             Width="224" MinHeight="208">
  <Grid Background="{DynamicResource PopupBackground}" Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="44"/>
      <RowDefinition/>
      <RowDefinition Height="48"/>
    </Grid.RowDefinitions>
    <RichTextBox Height="24" Margin="10,0" BorderBrush="{DynamicResource BaseColorOpacity40}"
                 VerticalAlignment="Center" VerticalContentAlignment="Center"/>
    <Grid VerticalAlignment="Center" Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition/>
        <ColumnDefinition/>
      </Grid.ColumnDefinitions>
      <Button Name="SaveButton" Content="{DynamicResource PublicSave}" Style="{StaticResource SaveButtonStyle}"
              Height="28" Width="86" FontSize="13" Grid.Column="0"/>
      <Button Content="{DynamicResource Cancel}" Style="{StaticResource CancelButtonStyle}" Width="86"
              Height="28" FontSize="13" Grid.Column="1"/>
    </Grid>
  </Grid>
</UserControl>