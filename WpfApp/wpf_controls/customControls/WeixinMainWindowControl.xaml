<UserControl x:Class="wpf_controls.customControls.WeixinMainWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpf_controls.customControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="53"/>
            <ColumnDefinition MinWidth="200" MaxWidth="400"/>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="Blue" BorderThickness="2">
            <Button Content="Test1"></Button>
        </Border>

        <Border Grid.Column="1" BorderBrush="Red" BorderThickness="2">
            <TextBlock Text="this is a test Text" FontSize="30"/>
        </Border>

    </Grid>
</UserControl>
