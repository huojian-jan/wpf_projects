<Window x:Class="wpf_controls.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf_controls"
        xmlns:controls="clr-namespace:wpf_controls.controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="150" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Rectangle Fill="Blue"/>

        </Grid>
        <Grid Grid.Column="1">
            <controls:DrawerContainer>
            <controls:Drawer x:Name="drawer" ShowMode="Conver" IsOpen="True">
                <StackPanel Orientation="Vertical">
                    <Button  Content="OK" Width="76" Height="28"/>
                    <Button  Content="OK" Width="76" Height="28"/>
                </StackPanel>
            </controls:Drawer>
            </controls:DrawerContainer>
        </Grid>

        <Grid Grid.Column="2">
            <Button Content="Change" Width="100" Click="ButtonBase_OnClick" Height="38" Margin="10"></Button>
        </Grid>

    </Grid>
</Window>