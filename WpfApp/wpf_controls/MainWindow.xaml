<Window x:Class="wpf_controls.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf_controls"
        xmlns:ctl="clr-namespace:wpf_controls.customControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="100" MaxWidth="300"/>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Name="leftDock" Grid.Row="0" Grid.Column="0" >
            <Button Name="leftButton" Click="LeftButton_OnClick" Content="expanded" FontSize="30" FontWeight="Bold"/>
        </Grid>

        <Grid Grid.Column="1" Grid.Row="0">
            <Rectangle Fill="Green"/>
        </Grid>
    </Grid>
</Window>
